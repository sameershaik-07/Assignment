{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for Full Stack Assignment",
  "main": "src/server.js",
  "scripts": {
    "start": "npx prisma generate && npx prisma migrate deploy && node src/server.js",
    "dev": "node src/server.js",
    "build": "npx prisma generate",
    "test": "jest --detectOpenHandles --forceExit"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.1",
    "prisma": "^7.9.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
